package resources.error.scheduling;

import resources.error.MasterError;

public class CollisionError 
							extends MasterError{

	public CollisionError(String errorMessage) {
		super(errorMessage);
		 
	}

}
